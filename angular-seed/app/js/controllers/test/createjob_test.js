'use strict';

describe('myApp.createJob module', function() {

  beforeEach(module('myApp.createJob'));

  describe('createJob controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('createJobCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});